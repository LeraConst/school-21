# validator.sh

validate_input() {
    input=$1

    # Проверка, является ли введенное значение числом (включая отрицательные и десятичные числа)
    if [[ $input =~ ^-?[0-9]+(\.[0-9]+)?$ ]]; then
        echo "Ошибка: Введенное значение не является корректным."
        exit 1
    fi
}

